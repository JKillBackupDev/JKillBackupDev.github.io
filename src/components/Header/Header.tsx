import React from "react";

type Props = {};

const Header: React.FC<Props> = () => {
	return (
		<div className='flex flex-row align-center justify-between p-[20px] w-full'>
		</div>
	);
};

export default Header;
